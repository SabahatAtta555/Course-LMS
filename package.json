{
  "name": "coursepilot-fullstack-lms",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack course LMS with authentication, enrollment, lessons, quizzes, progress tracking and certificates.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^5.1.0"
  }
}
